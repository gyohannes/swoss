<% if @category.try(:code) == 2 or @category.try(:code) == 3 %>
    <%= simple_fields_for :admission, @admission do |f| %>
        <%= f.input :priority, as: :boolean,  disabled: @admission.blank? ? false : f.object.persisted? %>
    <% end %>
<% end %>